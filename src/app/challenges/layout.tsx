import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Challenges — Win Token Prizes',
  description: "Join WePollit challenges and share in the token prize pool. Vote, participate and earn tokens on Nigeria's opinion platform.",
}

export default function ChallengesLayout({ children }: { children: React.ReactNode }) {
  return children
}
