import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sponsor a Poll — Reach Nigerian Audiences',
  description: 'Sponsor polls on WePollit and reach millions of engaged Nigerians. Get real data, demographic breakdowns and state-by-state insights.',
}

export default function SponsorLayout({ children }: { children: React.ReactNode }) {
  return children
}
